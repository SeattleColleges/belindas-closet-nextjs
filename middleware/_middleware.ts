// // middleware/_middleware.ts
// import { NextResponse, NextRequest } from 'next/server';

// // This function now directly returns a response for testing purposes
// export function middleware(request: NextRequest) {
//     console.log('Middleware is running');  // This log will show in the server console
//     return new Response('Middleware active', { status: 200 });
// }
